    OK = 0,
    HTTP_ERROR,
    FILE_ERROR,
    ABORTED,
                       const std::string& username, const std::string& password);
                       const std::string& username, const std::string& password);
                                      ProgressCallback progress = nullptr);
                                      const std::string& username, const std::string& password,
                                      ProgressCallback progress = nullptr);
